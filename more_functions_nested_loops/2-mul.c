#include "main.h"

/**
 * mul - a function that multiplies two integers
 *
 * @num1: a number to multiply with num2
 * @num2: the number to multiply with num1
 *
 * Return: 0 (is much success!)
 *         
 */

int mul(int num1, int num2)
{
	return (num1 * num2);
}
